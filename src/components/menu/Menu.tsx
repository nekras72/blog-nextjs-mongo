import React from "react";
import styles from './menu.module.css';

const Menu: React.FC = () => {
    return (
        <div className={styles.container}>
            Menu
        </div>
    )
};

export default Menu;
